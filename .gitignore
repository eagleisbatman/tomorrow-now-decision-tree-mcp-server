node_modules/
dist/
.env
.env.local
.env.*
*.log
.DS_Store
*.xlsx
*.xls

# Cursor rules (local only, not shared)
.cursorrules

# Security: Never commit credentials
*credentials*
*secrets*
*password*
*database-url*
*connection-string*
